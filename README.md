# Dispersion

- naming sense: based on the light(prism) decay(rust)
- no AI agent was ~~hurt~~ used when writing rust code(about anthing else that is not rust like github actions, well that is another thing)
